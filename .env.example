# Sova Testnet Configuration
# Copy file ini ke .env dan isi dengan nilai yang sesuai

# Private Key (dengan atau tanpa 0x prefix)
PRIVATE_KEY=0x

# Wallet Address (opsional, untuk referensi)
WALLET_ADDRESS=0x

# Groq API Key (untuk AI error monitoring dan analysis)
# Dapatkan dari: https://console.groq.com/keys
# Jika tidak diset, bot akan menggunakan rule-based analysis
GROQ_API_KEY=

# Amount to mint (100000000 = 1 sovaBTC karena 8 decimals)
AMOUNT=100000000

# RPC URL (opsional, default sudah ada)
RPC_URL=https://rpc.testnet.sova.io

# Contract Address (opsional, default sudah ada)
CONTRACT_ADDRESS=0x5Db496debB227455cE9f482f9E443f1073a55456

# Telegram Bot Token (dari @BotFather)
TELEGRAM_BOT_TOKEN=

# Allowed Users (opsional, kosongkan untuk semua user bisa akses)
# Format: user_id1,user_id2,user_id3
# Contoh: ALLOWED_USERS=123456789,987654321
ALLOWED_USERS=

# Wallet Encryption Key (32-byte hex string untuk mengenkripsi wallet.json)
# Generate dengan: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WALLET_ENCRYPTION_KEY=

# ===================================================================
# VAULT CONFIGURATION (Optional - for Sova Prime Vault Integration)
# ===================================================================
# Vault features are DISABLED by default. To enable vault commands
# (/vaultinfo, /vaultdeposit, /vaultwithdraw), you must configure:
#
# 1. Set contract addresses for your network:
# SPBTC_CONTRACT=0x...     # spBTC token contract address
# CONDUIT_CONTRACT=0x...   # Vault (tRWA) contract address
# VAULT_NETWORK=sova-testnet  # Optional: network name for logging
#
# Pre-configured networks (no manual config needed):
# - sepolia: Set VAULT_NETWORK=sepolia (uses known addresses)
#
# If not configured, vault commands will show "Service Unavailable"
# but all other bot features (minting, transfers, check-ins) work normally.
# ===================================================================
